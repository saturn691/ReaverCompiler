/*

For structure initalization

e.g.,
C code: struct Point {int x, y;} p = {1, 2};
The {1, 2} part is parsed by initializer_list (each number being an initializer),
and the whole {1, 2} is parsed by the initializer rule as a complex initializer.


*/
